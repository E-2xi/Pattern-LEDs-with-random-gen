# Pattern-LEDs-with-random-gen
all are almost pretty same except adding random generator on the test bench of ring_cnt file. 

The tb_ring_cnt_1 file is successfully worked as random generator on the reset := rand_int_stor. 
